services:
  jasper:
    build: .
    container_name: jasper
    ports:
      - "4278:3000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./prisma/prod.db:/app/prisma/prod.db
